import Foundation

enum ReviewGenerationDisclosure {
    static let title = "Image Use Notice"

    static let message = """
    In the review version, your uploaded images are used only to create the result you request. They are processed temporarily on our own servers, are not used to train models, and are not shared with third-party AI services.
    """
}
